#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (n); ++i)
using ll = long long;
using P = pair<int, int>;

int main() {
  string s; cin >> s;
  if (s[s.size()-1] == 'r') cout << "er" << endl;
  else cout << "ist" << endl;

  return 0;
}
